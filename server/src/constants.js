const API = '/api';

const AUTH_ROUTES = {
  SIGNUP: '/signup',
  USERS: '/users',
  LOGIN:'/login',
  LOGOUT:'/logout',
  ME:'/profile',
  AUTH:'/me',
};

const USER = `/user`;
const AUTH = `/auth`;

module.exports = {
  API,
  AUTH_ROUTES,
  USER,
  AUTH,
};
