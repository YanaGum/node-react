const API = '/api';

const AUTH_ROUTES = {
  SIGNUP: '/signup',
  USERS: '/users',
};

const { USER } = `/user`;
const { AUTH } = `/auth`;

module.exports = {
  API,
  AUTH_ROUTES,
  USER,
  AUTH,
};
